requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
openai>=1.40
pydantic>=2.0
python-dotenv>=1.0
ddgs>=6.0
cloudscraper>=1.2.71

# Optional: for JS-rendered pages (auto-fallback uses this as last resort)
# pip install playwright && playwright install chromium
playwright>=1.40
